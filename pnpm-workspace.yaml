packages:
  - "apps/*"
  - "packages/*"

# Packages allowed to run install scripts (pnpm 11)
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  bcrypt: true
  prisma: true
  # Optional eslint native resolver — JS fallback is enough
  unrs-resolver: false
