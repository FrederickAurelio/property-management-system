---
description: Constraints when editing apps/web
globs: apps/web/**
alwaysApply: false
---

# Web

- Phase 2. FE stack undecided. Books via shared `apps/api` only.
- Prefer `api` + `pms` until Phase 2 starts. Read `apps/web/AGENTS.md`.
