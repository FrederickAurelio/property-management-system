---
description: Constraints when editing apps/web
globs: apps/web/**
alwaysApply: false
---

# Web

- Phase 2. Prefer `api` + `pms` unless Phase 2 is explicitly started.
- Guest booking only; writes to shared `apps/api` (`source: website`).
- No separate booking DB. Read `apps/web/AGENTS.md` when Phase 2 is active.
