---
description: PMS entry — stack and phase when editing apps/pms
globs: apps/pms/**
alwaysApply: false
---

# PMS (entry)

Concern rules layer on via tighter globs (`pms-api`, `pms-ui`). Map: [`.cursor/rules/README.md`](README.md).

- React + Vite + Tailwind + shadcn. Staff ops only → `apps/api`.
- Calendar, reservations, check-in/out, quick-confirm first.
- Read `apps/pms/AGENTS.md`.
