---
description: Cabin PMS monorepo — architecture, stack, phase hard stops
alwaysApply: true
---

# Monorepo

- Apps: `api` (`@cabin/api` Nest), `pms` (`@cabin/pms` Vite/React/Tailwind/shadcn), `web` (Phase 2 scaffold).
- **pnpm only** from repo root. Never `npm i` inside an app.
- One API only. Product: `AGENTS.md` + `.docs/cabin-pms-client-plan.md`.
- Phase 1 = `api` + `pms`. No `web` booking, CM, or OTA scraping in Phase 1.
- Unique-unit calendars. Overlap safety in Postgres.
- Tooling pitfalls (pnpm builds, Prisma output, single `.env`, ESLint per app): `.cursor/rules/monorepo-tooling.mdc`.
- When editing `AGENTS.md`: rewrite cleanly — see root `AGENTS.md` → “How to write AGENTS.md”.
