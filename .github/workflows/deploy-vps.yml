# Primary deploy: build images on the VPS (docker compose build), then up + migrate.
# VPS .env: APT_MIRROR / NPM_REGISTRY for China mirrors; leave empty for SEA.
# GHCR backup (build on GitHub, pull on VPS): deploy-vps.ghcr.backup.yml
# Secrets: VPS_HOST, SSH_USER, SSH_PRIVATE_KEY
# Path: ~/property-management-system · branch: release

name: Deploy to VPS

on:
  push:
    branches: [release]
  workflow_dispatch:

concurrency:
  group: deploy-vps
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 150m
          script: |
            set -euo pipefail
            cd ~/property-management-system
            git fetch origin
            git checkout release
            git reset --hard origin/release

            echo "=== disk before build ==="
            df -h /
            docker system df || true

            # Free space for layer export (weak VPS can look "stuck" at exporting layers)
            docker image prune -f || true
            docker builder prune -f --keep-storage 1GB || true

            for img in cabin-api cabin-pms; do
              if docker image inspect "${img}:latest" >/dev/null 2>&1; then
                docker tag "${img}:latest" "${img}:previous"
              fi
            done

            export DOCKER_BUILDKIT=1
            export BUILDKIT_PROGRESS=plain
            export COMPOSE_HTTP_TIMEOUT=3600
            export DOCKER_CLIENT_TIMEOUT=3600

            docker compose build --progress=plain api
            docker compose build --progress=plain pms
            docker compose up -d
            docker compose exec -T api npx prisma migrate deploy

            for img in cabin-api cabin-pms; do
              docker images --format '{{.Repository}}:{{.Tag}}' "$img" | while read -r ref; do
                case "$ref" in
                  *:latest|*:previous) ;;
                  *) docker rmi "$ref" || true ;;
                esac
              done
            done
            docker image prune -f
            docker container prune -f
            docker builder prune -f --keep-storage 2GB
